#include <stdio.h>

int main() {
    // Write C code here
    int a,b,c;
    scanf("%d %d %d",&a,&b,&c);
    if(a<b && b<c){
        printf("a is small");
    }
    else if(b<c){
        printf("b is small");
    }else{
        printf("c is small");
    }

    return 0;
}
